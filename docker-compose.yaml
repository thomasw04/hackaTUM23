version: '3.9'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    links:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
