version: '3.9'

services:
  frontend:
    image: hackatum-2023-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    links:
      - backend

  backend:
    image: hackatum-2023-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
